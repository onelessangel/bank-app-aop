spring:
  profiles:
    active: dev

feed:
  filename: demo.feed

audit:
  amount:
    deposit: 99
    withdrawal: 99

logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure: ERROR
---

spring:
  profiles: dev
debug: true

---

spring:
  profiles: prod
debug: false
